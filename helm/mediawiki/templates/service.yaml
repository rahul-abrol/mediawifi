apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediawiki.fullname" . }}
  labels:
    app: {{ include "mediawiki.name" . }}
    chart: {{ include "mediawiki.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "mediawiki.name" . }}
    release: {{ .Release.Name }}
